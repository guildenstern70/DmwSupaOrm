# DMW SUPA ORM

DMW SUPA ORM is a lightweight Object-Relational Mapping (ORM) library for the course Data Management and WareHouse
master course at Università Cattolica di Milano.

It provides an easy-to-use interface for defining models, querying databases, and managing relationships between entities.

## Setup

To set up DMW SUPA ORM, follow these steps:

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
   
